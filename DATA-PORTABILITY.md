# Data portability

Export current project creates one portable project JSON. Download all local data creates a browser backup. Imports create a new workspace by default. Clear all data only removes WeaveStudio-owned browser keys and requires typing `CLEAR`; download a backup first because clearing cannot be undone.
