/** Re-export template registry for existing import paths. */
export {
  TEMPLATES,
  getTemplateById,
  getPrimaryTemplates,
  getLegacyTemplates,
  resolveTemplateId,
  PRIMARY_TEMPLATES,
  LEGACY_TEMPLATES,
  PRIMARY_TEMPLATE_IDS,
} from './templates/index';
