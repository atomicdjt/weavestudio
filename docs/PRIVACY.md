# Privacy boundaries

WeaveStudio stores workspace documents and snapshots in this browser under `weavestudio_*` keys. It has no account or cloud sync.

Optional AI assistance sends only the instruction and input context shown in the confirmation dialog, and only after an affirmative action. API keys are volatile tab memory, never exported, persisted, logged, or added to URLs. Generated output remains a reviewable draft until the user explicitly applies it.
